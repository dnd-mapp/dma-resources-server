export const DEFAULT_HOST = 'localhost';

export const DEFAULT_PORT = 3000;

export const MIN_PORT_RANGE = 0;

export const MAX_PORT_RANGE = 65535;

export const DEFAULT_MARIADB_USERNAME = 'root';

export const DEFAULT_MARIADB_HOST = 'localhost';

export const DEFAULT_MARIADB_PORT = 3306;

export const DEFAULT_MARIADB_SCHEMA = 'my_db';

export const EnvironmentVariableNames = {
    HOST: 'DMA_HOST',
    PORT: 'DMA_PORT',
    SSL_CERT_PATH: 'SSL_CERT_PATH',
    SSL_KEY_PATH: 'SSL_KEY_PATH',
    MARIADB_USERNAME: 'MARIADB_USERNAME',
    MARIADB_PASSWORD: 'MARIADB_PASSWORD',
    MARIADB_HOST: 'MARIADB_HOST',
    MARIADB_PORT: 'MARIADB_PORT',
    MARIADB_SCHEMA: 'MARIADB_SCHEMA',
};
